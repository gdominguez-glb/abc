$('body').append("<%= escape_javascript(render 'preview_modal') %>");
$("#preview_files_modal").modal('show');
$('#preview_files_modal').on('hidden.bs.modal', function (e) {
  $("#preview_files_modal").remove();
});
