<%= content_for :body_class, 'with-subnav' %>
<div class="row">
  <div class="col-sm-10 col-sm-offset-1 margin-top-xxl margin-bottom-xl">
    <%= render partial: 'curriculum_publications_nav', locals: { page: @group_page, publications: @curriculum_publications } %>
    <%= render partial: 'posts', locals: { posts: @posts, blog_type: 'curriculum', group_page: @group_page, publication: @medium_publication } %>
  </div>
</div>
