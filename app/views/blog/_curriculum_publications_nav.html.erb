<ul class="nav nav-tabs margin-bottom-m">
  <% publications.each do |publication| %>
    <li role="presentation">
      <%= active_link_to publication.title, curriculum_blog_path(page_slug: page.slug, slug: publication.slug) %>
    </li>
  <% end %>
</ul>
