<div class="col-md-6 video-grid">
  <div class="video-wrapper locked-video">
    <div class="image-drawer-wrapper flip-container">
      <div class="flipper">
        <div class="video-image-wrapper flip-front">
          <span class="glyphicon glyphicon-lock" aria-hidden="true"></span>
          <button class="btn btn-info flip-btn"><i class="fa fa-plus flip-icon"></i></button>
        </div>
        <div class="video-drawer blue-bg flip-back">
          <div class="product-description">
            <%= truncate video.description, length: 100 %>
            <p class="text-danger">Subscription Required</p>
          </div>

          <%= link_to unlock_video_gallery_path(video), class: 'btn btn-default btn-block', remote: true do %>
            Unlock Content
          <% end %>
          <button class="btn btn-info flip-btn"><i class="fa fa-minus flip-icon"></i></button>
        </div>
      </div>
    </div>
    <h6 class="video-name"><%= truncate(video.title, length: 50) %></h6>
    <div class="video-categories"><%= video.categories.join(", ") %></div>
    <div class="video-taxons"><%= video.taxons.map(&:name).join(", ") %></div>
  </div>
</div>
