<% if current_spree_user %>
  $("<%= params[:ref_id] %>").attr('href', "<%= bookmark_video_gallery_path(video, ref_id: "#bookmark-#{video.id}") %>");
  console.log(video);
  $("<%= params[:ref_id] %>").find('.fa').removeClass('fa-bookmark').addClass('fa-bookmark-o');
<% else %>
  alert('please login first.')
<% end %>
