<% @taxonomies.each do |taxonomy| %>
<div class="dropdown dropdown-md clear">
  <button class="btn btn-default btn-block dropdown-toggle dropdown-md-btn margin-bottom--m" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
    <%= taxonomy.name %>
    <i class="dropdown-md-mi mi md-24 toggle-arrow">keyboard_arrow_down</i>
  </button>
  <%= gm_taxons_tree(taxonomy.root, @taxon, taxonomy.allow_multiple_taxons_selected, :video_redesign) %>
</div>
<% end %>
