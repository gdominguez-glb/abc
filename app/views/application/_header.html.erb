<!-- Begin Navbar -->
<nav class="navbar navbar-default navbar-fixed-top">
  <div class="navbar-header">
    <a class="navbar-brand" href="/">
      Great Minds
    </a>
  </div>
  <button type="button" class="main-nav navbar-toggle collapsed btn pull-right" data-toggle="collapse" data-target="#main-nav">
    <span class="sr-only">Toggle navigation</span>
    <span class="glyphicon glyphicon-menu-hamburger"></span>
    <span class="glyphicon glyphicon-remove"></span>
  </button>
  <div class="collapse navbar-collapse clearfix" id="main-nav">
    <ul class="nav navbar-nav">
      <% Page.curriculum_nav.each do |page| %>
        <%= active_link_to page.group_name.capitalize, "/#{page.slug}", wrap_tag: :li %>
      <% end %>
      <li class="<%= store_active_class %>">
        <%= link_to "Shop", '/store', class: store_active_class %>
      </li>
    </ul>
    <div class="nav-right-options clearfix">
      <%= render partial: 'spree/shared/main_nav_bar' if store_menu? %>
    </div>
  </div>
</nav> <!-- /.navbar -->
<%= render 'secondary_nav' %>
