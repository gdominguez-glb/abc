<% if (request.path.start_with?('/cms') && !(controller_name == 'pages' && action_name == 'preview') ) || request.path.start_with?('/resources/admin') || (request.path.start_with?('/account') && ['products', 'resources', 'bookmarks', 'history', 'settings', 'licenses'].include?(controller_name)) %>
  <%= render 'products_nav' %>
<% elsif ['pages', 'blog', 'video_gallery', 'events'].include?(controller_name) %>
  <%= render "#{controller_name}_nav"  %>
<% end %>
