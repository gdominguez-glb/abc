<%= render 'cms_nav' %>

<div class="col-md-9">
  <%= render partial: 'search_bar', locals: { selected_state: 'drafts' } %>
  <div class="panel panel-primary">
    <div class="panel-heading padding-top--s padding-bottom--s">
      <h3 class="panel-title panel-title--s">All Draft Jobs</h3>
    </div>
    <%= render partial: 'job_list', locals: { state: 'drafts' } %>
  </div>
</div>
