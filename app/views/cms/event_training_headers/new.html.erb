<%= render 'cms_nav' %>
<div class="col-md-9">
  <h2>New sub header for <%= @training_type_category.title %></h2>

  <%= render 'form' %>

  <%= link_to 'Back', cms_training_type_category_event_training_headers_path(@training_type_category), class: 'btn btn-default' %>
</div>
