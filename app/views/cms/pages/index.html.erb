<%= render 'cms_nav' %>

<div class="col-md-9">
  <h2>Page Lists</h2>
  <%= link_to 'Create new page', new_cms_page_path, class: 'btn btn-primary' %>
  <p id="notice"><%= notice %></p>
  <%= search_form_for @q, url: cms_pages_path, class: 'form-inline' do |f| %>
    <div class="form-group">
      <%= f.text_field :title_or_slug_or_curriculum_name_cont, class: 'form-control' %>
    </div>
    <%= submit_tag 'Search', class: 'btn btn-primary' %>
  <% end %>
  <div class="table-responsive">
    <table class='table table-striped table-hover'>
    <thead>
      <tr>
        <th>Title</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @pages.each do |page| %>
        <tr>
          <td><%= page.title %></td>
          <td><%= cms_display_status(page) %></td>
          <td><%= link_to 'Edit Detail', edit_cms_page_path(page) %></td>
          <td><%= link_to 'Destroy', [:cms, page], method: :delete, data: { confirm: 'Are you sure?' }, class: 'text--danger' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  </div>
  <%= paginate @pages %>
</div>
