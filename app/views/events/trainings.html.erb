<div class="row">
  <div class="col-sm-10 col-sm-offset-1 margin-bottom-xl">

    <div class="page-header">
      <h2>Professional Development Sessions</h2>
    </div>

    <% @event_trainings.each do |event_training| %>
      <div class="panel panel-default panel-collapsible" id="event-<%= event_training.title_to_slug %>">
        <div class="panel-heading">
          <h4 class="panel-title">
            <a data-toggle="collapse" href="#collapse<%= event_training.title_to_slug %>"><%= event_training.title %></a>
          </h4>
        </div>
        <div id="collapse<%= event_training.title_to_slug %>" class="panel-collapse collapse padding-top padding-left padding-right">
          <p>
             <%= event_training.content.html_safe rescue nil %>
          </p>
        </div>
      </div>
    <% end %>
  </div>
</div>

<%= content_for :javascripts do %>
  <script type="text/javascript">
    $(function(){
      var anchor = window.location.hash;
      if (anchor) {
        $(anchor).find('[data-toggle="collapse"]').trigger('click');
      }
    });
  </script>
<% end %>
