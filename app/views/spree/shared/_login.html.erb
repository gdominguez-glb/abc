<%= form_for Spree::User.new, :as => :spree_user, :url => spree.create_new_session_path do |f| %>
  <div id="password-credentials">
    <div class="form-group has-icon-addon">
      <%= f.email_field :email, :class => 'form-control', :tabindex => 1, autofocus: true, placeholder: 'Email' %>
      <%= mi.person %>
    </div>
    <div class="form-group has-icon-addon">
      <%= f.password_field :password, :class => 'form-control', :tabindex => 2, placeholder: 'Password' %>
      <%= mi.lock %>
    </div>
  </div>
  <%# <p> %>
  <%#   <%= f.check_box :remember_me, :tabindex => 3 %1> %>
  <%#   <%= f.label :remember_me, Spree.t(:remember_me) %1> %>
  <%# </p> %>

  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= f.submit 'Sign in', :class => 'btn btn-lg btn-success btn-success--terracotta btn-block', :tabindex => 4 %>
    </div>
  </div>
<% end %>
